# Reference copy — the deploy workflow uses deploy/nginx-staging.conf.template
# (envsubst: STAGING_DOMAIN, NGINX_ROOT, NGINX_MEDIA_ROOT).
#
# GitHub Actions rsync:
#   main_web/dist/*  → NGINX_ROOT/
#   cms-admin/dist/* → NGINX_ROOT/cms/
